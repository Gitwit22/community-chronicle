# Frontend-only environment variables
# Point to the standalone Nxt Lvl Platform API.
# Include the /api prefix so client calls map directly to auth/documents endpoints.
VITE_API_BASE_URL=http://localhost:4000/api